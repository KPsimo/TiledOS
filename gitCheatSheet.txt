Pull from repo:

git pull origin main

-----------------------

Push to repo:

git add .
git commit -m "<describe what changed>"
git push origin main

-----------------------

Override local with remote:

git checkout main
git fetch origin
git reset --hard origin/main

Abbort Push:
git status (make sure you have unmerged paths)
git merge --abort